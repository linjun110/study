{% extends 'regular.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-4">
        </div>
        <div class="col-sm-4">
            <div class="well">

                {{ form_start(form) }}
                    <fieldset>
                        <legend><i class="fa fa-lock"></i> 注册 </legend>
                            <div class="form-group">
                {{ form_row(form.username) }}
                            </div>
                            <div class="form-group">
                {{ form_row(form.email) }}
                            </div>
                            <div class="form-group">
                {{ form_row(form.plainPassword.first) }}
                            </div>
                            <div class="form-group">
                {{ form_row(form.plainPassword.second) }}
                            </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-sign-in"></i> 注册
                        </button>
                    </fieldset>
                {{ form_end(form) }}


{#
                <form name="user" method="post">
                    <fieldset>
                        <legend><i class="fa fa-lock"></i> 注册 </legend>
                        <div class="form-group">
                            <label for="user_username">{{ 'label.username'|trans }}</label>
                            <input type="text" id="user_username" name="user[username]" required="required" class="form-control"/>
                        </div>
                        <div class="form-group">
                            <label for="user_email">邮箱</label>
                            <input type="email" id="user_email" name="user[email]" required="required" class="form-control"/>
                        </div>
                        <div class="form-group">
                            <label for="user_plainPassword_first">{{ 'label.password'|trans }}</label>
                            <input type="password" id="user_plainPassword_first" name="user[plainPassword][first]" required="required" class="form-control" />
                        </div>
                        <div class="form-group">
                            <label for="user_plainPassword_second">再次输入密码</label>
                            <input type="password" id="user_plainPassword_second" name="user[plainPassword][second]" required="required" class="form-control" />
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="fa fa-sign-in"></i> 注册
                        </button>
                        <input type="hidden" id="user__token" name="user[_token]" value=""/>
                    </fieldset>
                </form>
                #}

            </div>
        </div>
        <div class="col-sm-4">
        </div>
    </div>
{% endblock%}